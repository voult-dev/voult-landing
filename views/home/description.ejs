<% layout('layout/boilerplate.ejs') %>

<h3>What is voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>Why use voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How does voult.dev work?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How can I use voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How can I get started with voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How can I get help with voult.dev?</h3>     
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How can I contact voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>

<h3>How can I contact voult.dev?</h3>
<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>     

<h3>How can I contact voult.dev?</h3>

<p>voult.dev is a developer-first authentication platform — secure APIs,
   lightweight SDKs, OAuth, magic links, JWT handling, and pre-built UI
   kits. Skip the auth grind. Ship the product.</p>     